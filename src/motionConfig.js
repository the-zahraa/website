// src/motionConfig.js
export const motionViewportConfig = {
    once: true, // Prevent animations from re-triggering
    amount: 0.5, // Trigger animation only when 50% of the element is in view
    margin: '-50px', // Margin for viewport detection
  };
  
  export default motionViewportConfig;